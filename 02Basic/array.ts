const superHeros:string[] = []
// const heroPower: number[]=[]
const heroPower: Array<number> = []    // we can also write it in this way both are same 

type User = {
    name:string
    isActive: boolean
}

const allUsers: User[] = []
//we can writ this as this way also
const MLModels: number[][] = [
    [252, 254, 879],
    []
]

superHeros.push("batman")
heroPower.push(2)
allUsers.push({name:"", isActive:true})


export{}